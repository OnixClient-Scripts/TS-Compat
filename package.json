{
  "name": "TS-Compat",
  "version": "1.0.0",
  "description": "Type definitions for Onix Scripting.",
  "scripts": {
    "build": "tstl",
    "setup": "move ./*.json ../ && mkdir .\\Modules && mkdir .\\Commands && mkdir .\\Libs",
    "update": "cd ./TS-Compat && git pull && cd .."
  },
  "keywords": [],
  "author": "CreeperG16",
  "license": "MIT",
  "devDependencies": {
    "@typescript-to-lua/language-extensions": "^1.0.0",
    "lua-types": "^2.13.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6",
    "typescript-to-lua": "^1.17.0"
  }
}
